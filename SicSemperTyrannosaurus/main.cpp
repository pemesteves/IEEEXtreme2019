#include <iostream>
#include <vector>

using namespace std;

int main() {
    unsigned int n, k, q;
    cin >> n >> k >> q;
    
    unsigned long int vi;
    vector<unsigned long int> v
    while(n > 0){
        cin >> vi;
        v.push_back(vi);
        n--;
    }
    char beginning;
    unsigned long int l, r, p, x; 
    while(q > 0){
        cin >> beginning;
        if(beginning == 'Q'){
            cin >> l >> r;
            unsigned long int vr;
            for(unsigned int )
        }
        else{ //'U'
            cin >> p >> x;
            v[p] = x;
        }
        q--;
    }
    cout << a + b;
    return 0;
}